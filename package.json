{
  "name": "html-collection",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "build:app": "node scripts/build-subapp.mjs",
    "build:apps": "npm run build:app -- --all",
    "build": "npm run build:apps && astro build && node scripts/minify-dist.mjs && node scripts/generate-edgeone.mjs",
    "preview": "astro preview --host 0.0.0.0"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.6"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "html-minifier-terser": "^7.2.0",
    "svgo": "^4.0.1",
    "terser": "^5.46.2"
  }
}
