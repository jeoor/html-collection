{
  "name": "html-collection",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-works.mjs && vite --host 0.0.0.0",
    "build": "node scripts/generate-works.mjs && vite build && node scripts/generate-sitemap.mjs && node scripts/minify-dist.mjs",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "vite": "^5.1.0"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.46.2"
  }
}
